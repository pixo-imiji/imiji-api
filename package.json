{
  "name": "@imiji/api",
  "version": "0.0.10",
  "description": "",
  "author": "Anas Saber",
  "scripts": {
    "fix": "rm -rf node_modules && rm package-lock.json && npm install",
    "build": "rm -rf dist && tsc -p tsconfig.build.json"
  },
  "devDependencies": {
    "typescript": "^4.9.5"
  }
}
